version: '3.2'
services:
    nereid:
        image: nereid
        container_name: nereid
        environment:
            REDIS_SERVER: ${REDIS_SERVER}
            REDIS_PORT: ${REDIS_PORT}
            REDIS_AUTH: ${REDIS_AUTH}
        ports:
            - 8080:8080
